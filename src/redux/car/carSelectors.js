export const carsData = (state) => state.car.carList;
export const favoriteCarsData = (state) => state.car.favoriteCars;
export const page = (state) => state.car.page;
export const selectedCar= (state) => state.car.selectedValue;
